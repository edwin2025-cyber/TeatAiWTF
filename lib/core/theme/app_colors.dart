import 'package:flutter/material.dart';

/// App color constants for WeTheFuture
class AppColors {
  // Prevent instantiation
  AppColors._();

  // Light Theme Colors
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color lightCardSurface = Color(0xFFF4F7FB);
  static const Color lightTextPrimary = Color(0xFF050814);
  static const Color lightTextSecondary = Color(0xFF6B7280);
  static const Color lightBorderSubtle = Color(0xFFE5E7EB);

  // Dark Theme Colors
  static const Color darkBackground = Color(0xFF000000);
  static const Color darkCardSurface = Color(0xFF050814);
  static const Color darkTextPrimary = Color(0xFFF9FAFB);
  static const Color darkTextSecondary = Color(0xFF9CA3AF);
  static const Color darkBorderSubtle = Color(0xFF1F2933);

  // Shared Accent Colors
  static const Color accentPrimary = Color(0xFF0A84FF);
  static const Color accentSubtleLight = Color(0xFFCFE4FF);
  static const Color accentSubtleDark = Color(0xFF102542);

  // Status Colors
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFFACC15);
  static const Color destructive = Color(0xFFEF4444);

  // Additional colors
  static const Color navBarDark = Color(0xFF111827);
}
